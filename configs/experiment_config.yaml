# ============================================================================
# 👤 PERSON C: 파일 3: configs/experiment_config.yaml
# ============================================================================

"""
YAML 설정 파일 내용:
"""

# 모델 설정
model_name: "facebook/wav2vec2-xls-r-300m"  # or 1B 등
num_regions: 6
num_genders: 2

# 학습 설정
learning_rate: 5e-5
batch_size: 8  # Colab에서 8 권장
num_epochs: 30
weight_decay: 0.01

# 손실함수 가중치
region_loss_weight: 0.8  # 메인 태스크
gender_loss_weight: 0.2  # 보조 태스크

# Early stopping
patience: 5  # validation loss 개선 안 되면 5 에포크 후 중단

# 로깅
log_interval: 100  # step마다 로깅
save_interval: 1   # epoch마다 저장

# 데이터
train_ratio: 0.8
val_ratio: 0.1
test_ratio: 0.1

# 저장 경로
output_dir: "./checkpoints"
log_dir: "./logs"
